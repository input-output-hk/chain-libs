name: Continuous integration

on:
  push:
    branches:
      - master
    branches-ignore:
      # reserved for automatically updated branches; keep this rule to remember
      - 'ci/**'
  pull_request:

jobs:
  test:
    name: Test
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:

      - uses: actions/checkout@v2

      - name: Fetch baseline Cargo.lock
        run: |
          git fetch --depth=2 origin ci/cargo-lock
          git cherry-pick FETCH_HEAD

      - name: Cache cargo registry
        uses: actions/cache@v1
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-v1-${{ hashFiles('**/Cargo.toml') }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-v1-${{ hashFiles('**/Cargo.toml') }}-
            ${{ runner.os }}-cargo-registry-v1-

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
          components: rustfmt, clippy

      - name: Check code formatting
        uses: actions-rs/cargo@v1
        continue-on-error: false
        with:
          command: fmt
          args: --all -- --check

      - name: Check build
        uses: actions-rs/cargo@v1
        continue-on-error: false
        with:
          command: check

      - name: Run test suite
        uses: actions-rs/cargo@v1
        continue-on-error: false
        with:
          command: test

      - name: Check clippy lints
        uses: actions-rs/cargo@v1
        continue-on-error: false
        with:
          command: clippy
          # args: -- -D warnings # TODO: uncomment when ready to enforce all warning lints
